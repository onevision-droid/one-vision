{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "custom-variant", "apply", "tailwind", "layer", "config", "source", "plugin", "utility"]
      }
    ],
    "import-notation": "string"
  }
}
